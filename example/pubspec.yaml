name: example
description: A new Flutter project.
version: 1.3.0
publish_to: none

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  bdlogging:
    path: ..

dev_dependencies:
  flutter_code_style: ^2.2.0
